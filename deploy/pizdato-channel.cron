# Generated by deploy/install-channel.sh — do not hardcode personal paths here.
# This file is a placeholder; install-channel.sh writes /etc/cron.d/pizdato-channel.
SHELL=/bin/bash
PATH=/usr/local/bin:/usr/bin:/bin
MAILTO=""

# Morning: live stats + wisdom (Europe/Berlin on the VPS)
# 0 10 * * * DEPLOY_USER cd /opt/pizdato/channel && NODE post-daily.mjs >> /var/log/pizdato-channel.log 2>&1

# Evening: hot news as пиздато / хуёво
# 0 17 * * * DEPLOY_USER cd /opt/pizdato/channel && NODE post-evening.mjs >> /var/log/pizdato-channel.log 2>&1
