# Copy useful keys to ~/.config/pizdato-channel.env (not committed).
# Telegram API id/hash usually live in ~/.config/telegram-mcp.env

# Required for posting (Telegram user account that owns the StoreSession):
# TELEGRAM_ACCOUNT_ID=
# Optional session layout overrides:
# TELEGRAM_SESSION_CWD=
# TELEGRAM_SESSION_NAME=

# Evening posts prefer cursor-agent (already logged in on this host).
# PIZDATO_USE_CURSOR_AGENT=1
# PIZDATO_CURSOR_MODEL=
# PIZDATO_CURSOR_TIMEOUT_MS=180000

# Optional API fallbacks if cursor-agent is unavailable:
# OPENROUTER_API_KEY=
# GROQ_API_KEY=
# OPENAI_API_KEY=
# PIZDATO_LLM_MODEL=

# PIZDATO_CHANNEL=@pizdato_net
