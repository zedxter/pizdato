## Context

Улучшить существующий canvas-бейдж, чтобы он работал как виральный инструмент.

## Goals

1. **Улучшить дизайн бейджа** — сделать его визуально привлекательнее, проверить шрифты, цвета, композицию
2. **Добавить формат для сторис** — вертикальный 540×960 под Telegram/Instagram
3. **Замкнуть виральный цикл** — добавить `pizdato.net` на бейдж
4. **Добавить трекинг** — сколько скачиваний, сколько шарингов

## Decisions

### 1. Аудит текущего бейджа

- Открыть `badge.ts` и проверить, что рендерится сейчас
- Оценить качество: шрифты (Manrope, Bebas Neue), цвета, glow, отступы
- Решить, что улучшать (контрастность, читаемость, привлекательность)

### 2. Подпись `pizdato.net`

- Разместить в правом нижнем углу, мелким шрифтом (11px), полупрозрачным (#9aab9e)
- Не кликабельно (это картинка)
- **Верхний хедер:** убрать `PIZDATO.NET`, оставить только «МУДРОСТЬ ДНЯ» — иначе дубляж (ссылка на сайт и сверху и снизу)

### 3. Пилюли с процентами — референс `badge-horizontal-v1.png`

Расположение и шрифты пилюль должны строго соответствовать горизонтальному бейджу `openspec/changes/viral-badge/assets/badge-horizontal-v1.png`:

**Референсы дизайна (assets/):**

| Файл | Вариант | Формат | Размер PNG |
|------|---------|--------|-----------|
| `badge-horizontal-v1.png` | «Я — ЗА ПИЗДАТО!» | горизонтальный | 1280×720 (16:9) |
| `badge-horizontal-huyevo-v1.png` | «Я — ЗА ХУЁВО!» | горизонтальный | 1280×720 (16:9) |
| `badge-vertical-v1.png` | «Я — ЗА ПИЗДАТО!» | вертикальный | 720×1280 (9:16) |
| `badge-vertical-huyevo-v1.png` | «Я — ЗА ХУЁВО!» | вертикальный | 720×1280 (9:16) |

> **Примечание:** PNG-референсы — визуализации дизайна в высоком разрешении (масштаб ×1.78 для горизонтального). Canvas рендерится в 720×280 / 540×960 — те же пропорции и композиция, пилюли визуально идентичны.

**Расположение:**
- Две пилюли в нижней части бейджа, на одной линии по горизонтали
- Первая (зелёная) — «пиздато N%» — прижата к левому краю
- Вторая (красная) — «хуёво M%» — через отступ 10px от первой
- Обе пилюли **строго одинаковой ширины и высоты** (независимо от длины текста)
- Высота пилюли: 26px, скругление углов: 6px

**Шрифты:**
- Текст внутри пилюли: `800 15px Manrope, system-ui, sans-serif`
- Цвет текста — `#3dff9a` для «пиздато», `#ff4d3d` для «хуёво»
- Горизонтальный паддинг внутри пилюли: 11px слева и справа
- Вертикальное выравнивание текста по центру пилюли

**Цвета:**
- «пиздато»: фон — градиент от `#06100b` до `#0f8f52`, текст — `#3dff9a`
- «хуёво»: фон — градиент от `#1a0503` до `#a31f14`, текст — `#ff4d3d`

**Логика:**
- Проценты рассчитываются: `pizdato_pct = round(pizdato / total * 100)`, `huyevo_pct = round(huyevo / total * 100)`
- Если total == 0 — обе пилюли показывают 0%
- Всегда показываются обе пилюли, даже если одна из них 0%

### 4. Трекинг

- `navigator.sendBeacon('/api/event?type=badge_download')` при скачивании
- `/api/event?type=badge_share_stories` при скачивании для сторис
- В дайджесте (6:00): «скачиваний бейджа: N»
- Путь `/api/event` — через существующий Rust-бэкенд (127.0.0.1:8080), Caddy проксирует `/api/*`

### 5. Вертикальный формат (540×960)

- Отдельная функция `renderVerticalBadgeCanvas()` в `badge.ts`
- Расположение: хедер → выбор → цитата → проценты → подпись
- Кнопка «Сторис» в SharePanel

### 6. Как пользователь скачивает бейдж

После голосования в SharePanel пользователь видит **две кнопки скачивания**:

| Кнопка | Что скачивает | Формат | Имя файла |
|--------|--------------|--------|-----------|
| **«Скачать бейдж»** (существующая) | Горизонтальный 720×280 | PNG | `pizdato-{choice}-badge.png` |
| **«Сторис»** (новая) | Вертикальный 540×960 | PNG | `pizdato-{choice}-stories.png` |

- `{choice}` = `pizdato` (Я — ЗА ПИЗДАТО!) или `huyevo` (Я — ЗА ХУЁВО!)
- Обе кнопки в SharePanel, рядом друг с другом, одинакового стиля
- Скачивание через `downloadBadge()` (существующая) и `downloadVerticalBadge()` (новая)
- После скачивания пользователь сам делится картинкой где хочет (Telegram, сторис, форумы)

### 7. Отображение на сайте (по умолчанию)

**В SharePanel на сайте по умолчанию рендерится горизонтальная версия бейджа (720×280)** — как сейчас.

- SharePanel показывает превью горизонтального бейджа (canvas → blob → img), это уже реализовано в `useEffect` в SharePanel.tsx
- Вертикальная версия **не отображается на сайте** — она только для скачивания через кнопку «Сторис»
- Логика выбора цвета/текста: `{choice}` берётся из `stats.choice` (уже реализовано в badge.ts)
- Если голос ещё не сделан — бейдж показывает «ВЫБЕРИ СТОРОНУ» (существующее поведение)

### 8. Дизайн-ревью

- Перед мержем запостить скриншоты обоих форматов в PR
- Собрать фидбек от команды

## Mockups (text)

**Горизонтальный (720×280):**
```
┌──────────────────────────────────────────────────┐
│ МУДРОСТЬ ДНЯ                          хуёво        │
│                                                    │
│           Я — ЗА ПИЗДАТО!                         │
│                                                    │
│  «Код скомпилился — фича не работает.             │
│   Шрёдингер релиза.»                              │
│                                       — Марк      │
│                                                    │
│ [пиздато 67%] [хуёво 33%]              pizdato.net │
└──────────────────────────────────────────────────┘
```

**Вертикальный (540×960):**
```
┌──────────────────────────────┐
│                              │
│        МУДРОСТЬ ДНЯ          │
│                              │
│                              │
│       Я — ЗА ПИЗДАТО!       │
│                              │
│  «Код скомпилился — фича     │
│   не работает. Шрёдингер     │
│   релиза.»                   │
│                              │
│                    — Марк    │
│                              │
│                              │
│    [пиздато 67%] [хуёво 33%] │
│           pizdato.net        │
│                              │
└──────────────────────────────┘
```

## Open Questions (answered)

- **Дизайн:** изучить best practices баджей — где люди их используют (задача T0)
- **Трекинг:** защита от накрутки аналогично /vote — rate limiting: не более 10 событий в минуту с одного IP
